#lang racket

(provide db-host db-port db-user db-passwd db-schema)

(define db-host "localhost")
(define db-port 3306)
(define db-user "username")
(define db-passwd "password")
(define db-schema "flying")
